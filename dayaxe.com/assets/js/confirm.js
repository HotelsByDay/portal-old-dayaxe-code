$(document).ready(function () {
    //$.jCookie('CheckInDate', null);
    //$.jCookie('CurrentTicket', null);
    //$.jCookie('CurrentTicketValue', null);
    //$('#checkindate input').change(function() {
    //    $(this).removeClass('not-selected');
    //    $.jCookie('ChangeSelectedCheckInDate', $(this).val());
    //    $('#checkindate .input-group-addon').text('Change');
    //});

    //if ($.jCookie('ChangeSelectedCheckInDate')) {
    //    $('#checkindate input').val($.jCookie('ChangeSelectedCheckInDate')).removeClass('not-selected');
    //    $('#checkindate .input-group-addon').text('Change');
    //}
});